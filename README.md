# Cats API

To run:

1. Ensure there is a folder called `.persist` in the project root
2. Run `npm install`
3. Run `npm start`

Go to http://localhost:3000/swagger to see the swagger docs for this API
